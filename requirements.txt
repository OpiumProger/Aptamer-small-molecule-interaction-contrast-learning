# pip-зависимости для проекта Contrast Learning
# Установка: pip install -r requirements.txt
#
# Рекомендуется сначала установить PyTorch с https://pytorch.org
# (версия под вашу CUDA), затем остальное:
#   pip install torch torchvision torchaudio
#   pip install -r requirements.txt

# --- Core ---
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65

# --- Deep learning ---
# torch>=2.0          # установите отдельно под вашу платформу/CUDA
transformers>=4.36
safetensors>=0.4
huggingface-hub>=0.20
tokenizers>=0.15

# --- Embeddings / viz ---
umap-learn>=0.5

# --- GRU generation ---
python-Levenshtein>=0.21

# --- Wasserstein analysis ---
POT>=0.9

# --- Optional (boltz_script.py plots) ---
# matplotlib уже выше

# --- NOT included (install separately) ---
# boltz              # Boltz-2 CLI, separate environment
# vina py3Dmol       # docking, Google Colab only
# openbabel          # docking prep
